server:
  port: 8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
  instance:
    instance-id: http://${eureka.instance.hostname}:${server.port}/eureka/